{
  "name": "ratels",
  "version": "0.1.0",
  "description": "Supply chain security monitor for package installs — snapshots your system before/after an install (or on demand via `ratels scan`) and flags anything suspicious.",
  "bin": {
    "ratels": "./cli.js"
  },
  "main": "cli.js",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}